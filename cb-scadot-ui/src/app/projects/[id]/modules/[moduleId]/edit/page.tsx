'use client'
import React from 'react';

export default function ModuleEditorPage({ 
    params 
}: { 
    params: { id: string; moduleId: string } 
}) {
    return (
        <div>Module Editor Page</div>
    );
}
