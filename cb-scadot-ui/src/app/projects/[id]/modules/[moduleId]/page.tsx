'use client'
import React from 'react';

export default function ModuleDetailsPage({ 
    params 
}: { 
    params: { id: string; moduleId: string } 
}) {
    return (
        <div>Module Details Page</div>
    );
}
